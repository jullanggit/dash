[package]
name = "dash"
version = "0.1.0"
authors = ["Jul Lang <jullanggit@proton.me>"]
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
charming = { version = "0.6.0", optional = true }
dioxus = { version = "0.7.1", features = ["router", "fullstack"] }
futures = { version = "0.3.32", default-features = false, optional = true }
rspotify = { version = "0.15.3", optional = true, default-features = false, features = ["client-reqwest", "reqwest-rustls-tls", "cli", "env-file"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.149"
structstruck = "0.5.1"
time = { version = "0.3.47", features = ["serde", "parsing"] }
tokio = { version = "1.50.0", features = ["fs"], optional = true }

wasm-bindgen = "=0.2.108" # corresponds to installed version. Can be removed if 'dx serve' works without it.

[features]
default = ["server"]
# The feature that are only required for the web = ["dioxus/web"] build target should be optional and only enabled in the web = ["dioxus/web"] feature
web = ["dioxus/web"]
# The feature that are only required for the desktop = ["dioxus/desktop"] build target should be optional and only enabled in the desktop = ["dioxus/desktop"] feature
desktop = ["dioxus/desktop"]
# The feature that are only required for the mobile = ["dioxus/mobile"] build target should be optional and only enabled in the mobile = ["dioxus/mobile"] feature
mobile = ["dioxus/mobile"]
# The feature that are only required for the server = ["dioxus/server"] build target should be optional and only enabled in the server = ["dioxus/server"] feature
server = ["dioxus/server", "dep:tokio", "dep:futures", "dep:charming", "dep:rspotify" ]
